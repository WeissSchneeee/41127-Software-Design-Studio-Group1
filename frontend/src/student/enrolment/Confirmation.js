import React from "react";

export const Confirmation = (userID, subject) => {
    return(
        <section id="confirmationForm" className="form-popup center form-container">
            <h2>ENROLMENT CONFIRMATION</h2>
            
        </section>
    );
};